name: 2 Workflow
on:  
  issues:
    types: [opened, edited, milestoned]
jobs:
  TestJob:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Context
        run: echo "${{ toJSON(github.event) }}"
